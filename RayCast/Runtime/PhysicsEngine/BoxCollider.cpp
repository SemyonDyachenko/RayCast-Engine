#include "../../stdafx.h"
#include "BoxCollider.h"

/*
BoxCollider::BoxCollider(glm::vec3 size, glm::vec3 position, glm::vec3 rotation)
	: Size(size),
	Position(position)
{
	RotationToQuat(rotation);
}

void BoxCollider::RotationToQuat(glm::vec3 rotation)
{
	glm::vec3 angleRotation = glm::vec3(glm::radians(rotation.x), glm::radians(rotation.y), glm::radians(rotation.z));

	Rotation = glm::quat(angleRotation);
}
*/